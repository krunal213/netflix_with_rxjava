<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NetflixApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NetflixWithRxJava"
        tools:targetApi="31">
        <activity
            android:name=".view.activity.SplashActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.GetStartedActivity"
            android:exported="true"/>
        <activity
            android:name=".view.activity.NetflixMainActivity"
            android:exported="true"/>
        <activity
            android:name=".view.activity.SearchActivity"
            android:exported="true"
            android:label="">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".view.activity.AppSettingsActivity"
            android:exported="true"
            android:label="App Settings"/>
        <activity
            android:name=".view.activity.HelpActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.NetflixWithRxJava.White"/>
    </application>

</manifest>